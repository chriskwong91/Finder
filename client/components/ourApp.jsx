let React = require('react');
let RouteHandler = React.RouteHandler;

export class ourApp extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <RouteHandler />
    );
  }
}
